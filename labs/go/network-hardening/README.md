# Network hardening assessment

This Go lab converts the course scenario into a reproducible Markdown assessment.
It reuses the repository's evidence-first reporting style and covers the four
vulnerabilities described in the activity. The output is a scenario-derived draft,
not a platform submission or production assessment.

```bash
go run ./labs/go/network-hardening | tee network-hardening-assessment.md
```
